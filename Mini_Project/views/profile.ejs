<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center">

  <!-- Navbar -->
  <div class="w-full bg-white shadow-md flex justify-between items-center px-6 py-3 mb-6">
    <h1 class="text-xl font-bold text-pink-500">Grammify</h1>
    <a href="/logout" class="bg-red-500 text-white px-3 py-1.5 rounded hover:bg-red-600 transition">Logout</a>
  </div>

  <!-- Create Post Card -->
  <div class="bg-white w-full max-w-md rounded-lg shadow p-4 mb-6">
    <h3 class="text-gray-800 font-semibold mb-3">What's on your mind, <%= user.username %>?</h3>
    <form action="/post" method="POST" class="flex flex-col gap-3">
      <textarea name="content" rows="3" class="w-full p-3 border border-gray-300 rounded focus:ring-2 focus:ring-pink-500 outline-none" placeholder="Share something..."></textarea>
      <button type="submit" class="bg-pink-500 text-white py-2 rounded font-medium hover:bg-pink-600 transition">Post</button>
    </form>
  </div>

  <!-- Posts Feed -->
  <div class="w-full max-w-md flex flex-col gap-6">
    <% if (user.posts && user.posts.length > 0) { %>
      <% user.posts.reverse().forEach(post => { %>
        <div class="bg-white rounded-lg shadow p-4 border border-gray-200">
          
          <!-- Post Header -->
          <div class="flex justify-between items-center mb-2">
            <h4 class="font-semibold text-gray-800">@<%= user.username %></h4>
            <p class="text-xs text-gray-400"><%= post.date.toDateString() %></p>
          </div>

          <!-- Post Content -->
          <p class="text-gray-800 mb-3"><%= post.content %></p>

          <!-- Post Actions -->
          <div class="flex gap-4 text-sm">
            <a href="/like/<%= post._id %>" class="text-pink-500 hover:text-pink-400 transition">
              ❤️ Like (<%= post.likes.length %>)
            </a>
            <a href="/edit/<%= post._id %>" class="text-blue-500 hover:text-blue-400 transition">
              ✏️ Edit
            </a>
          </div>

        </div>
      <% }) %>
    <% } else { %>
      <p class="text-gray-500 text-center">No posts yet. Start sharing your thoughts!</p>
    <% } %>
  </div>

</body>
</html>
